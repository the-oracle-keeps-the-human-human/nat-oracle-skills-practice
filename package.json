{
  "name": "nat-oracle-skills-practice",
  "version": "0.1.0",
  "description": "Practice building Oracle skills — learn by doing",
  "type": "module",
  "bin": {
    "nat-oracle-skills": "./src/cli/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/cli/index.ts --outdir dist --target bun --minify",
    "dev": "bun run src/cli/index.ts",
    "test": "bun test __tests__/",
    "compile": "bun scripts/compile.ts"
  },
  "keywords": [
    "oracle",
    "skills",
    "practice",
    "claude-code",
    "ai-agents",
    "bun"
  ],
  "author": "Nat Weerawan",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/the-oracle-keeps-the-human-human/nat-oracle-skills-practice.git"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "commander": "^12.0.0"
  }
}
